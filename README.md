# MIGRATION-GUIDLINE-FOR-ANYONE-TO-RUST

process
1. Write the C++ programs thats meaning-level semantics translation program(might be meta-programming).
2. Write the Network programs that is to be newer computers(When On Enterprise level, Empty Server, the newer one will be needed).
3. Choose your your programs step-by-steps. It must be functionalities units and must be automatically.
4. Translate the meaning into Rust, that is step-by-step(may need the buffer).
5. Rust be compiled to assembly language, however, thats not with llvm. Cause the LLVM is too large and not proper choice for the resources, in spite of the resource is plentiful.
6. Assembly language Must be translate into the each-machine's binary codes.
